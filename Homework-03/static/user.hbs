<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <style>
        .wrapper{
            width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-wrap: wrap;
        }
        .container{
            width: 250px;
            border: 1px solid brown;
            padding: 15px;
            margin: 5px;
            border-radius: 5px;
            box-shadow: 1px 1px 2px brown;
        }
        .items{
            padding: 10px;
        }
        input{
            padding: 5px 50px;
            cursor: pointer;
        }
    </style>
    <title>User</title>
</head>
<body>
<form action="/login">
    <input type="submit" value="Перейти на страницу регистрации">
</form>
<div class="wrapper">
    <div class="container">
        <div class="items">firstName:<b> {{user.firstName}}</b></div>
        <div class="items">lastName:<b> {{user.lastName}}</b></div>
        <div class="items">age:<b> {{user.age}}</b></div>
        <div class="items">city:<b> {{user.city}}</b></div>
        <div class="items">email:<b> {{user.email}}</b></div>
        <div class="items">password:<b> {{user.password}}</b></div>
    </div>
</div>
</body>
</html>